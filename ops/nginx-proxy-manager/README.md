# Nginx Proxy Manager

Reverse proxy for routing traffic to services with SSL termination.

## Setup

```bash
docker compose up -d
```

Admin UI: `http://<host>:81`

Default credentials: `admin@example.com` / `changeme`
